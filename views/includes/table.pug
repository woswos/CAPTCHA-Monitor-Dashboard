div(class='row no-gutters')
	div(class='col-md')
		table(id='search_table' class='table table-striped table-bordered')
			thead
				tr
					th(scope='col') ID
					th(scope='col') Timestamp (UTC)
					th(scope='col') Method
					th(scope='col') 
						| Tor Browser 
						br 
						| Security Level
					th(scope='col') URL
					th(scope='col') Exit Node
					th(scope='col') 
						| Faced
						br
						| CAPTCHA ?
			tbody

// Center the table cells only here
style.
	.table td, th {
		text-align: center;
	}
	.table {
		width: 100%;
	}
